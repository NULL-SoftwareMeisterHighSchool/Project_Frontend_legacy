export type patchEditPasswordProps = {
    currentPassword: string;
    newPassword: string;
};

export type putEditMeProps = {
    bio: string;
    stacks: string[];
    githubURL: string;
    portfolioURL: string;
};
